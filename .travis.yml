language: generic
sudo: required
services: 
  - docker: 

before_install:
  - docker build -t meet3374/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true meet3374/docker-react npm run test